export const scrollToTop = () => {
    window.scrollTo({
        top: 0,
        behavior: 'auto',
    });
};

export const scrollToTopAnimation = () => {
    window.scrollTo({
        top: 0,
        behavior: 'smooth',
    });
};

